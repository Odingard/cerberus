/**
 * LangChain Adapter — Planned
 *
 * Framework-specific integration for LangChain agents.
 * Will wrap LangChain's tool calling interface to Cerberus guard().
 *
 * Status: Planned. Use guard() directly with custom ToolExecutorFn wrappers.
 *
 * Depends on: src/middleware/wrap.ts
 */

/** Placeholder configuration type for the LangChain adapter. */
export type LangChainAdapterConfig = Record<string, never>;
