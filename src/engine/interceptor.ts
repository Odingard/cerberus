/**
 * Interceptor — ALERT and INTERRUPT logic.
 *
 * Receives risk assessments from the correlation engine and
 * executes the configured action (log, alert, or interrupt).
 *
 * Depends on: src/types/signals.ts, src/types/config.ts
 */

export {};
