/**
 * Layer 2 — Token Provenance Tagger
 *
 * Labels every context token by origin before the LLM call.
 * Emits: UNTRUSTED_TOKENS_IN_CONTEXT signal.
 *
 * Depends on: src/types/signals.ts
 */

export {};
