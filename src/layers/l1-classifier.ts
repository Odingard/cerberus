/**
 * Layer 1 — Data Source Classifier
 *
 * Tags every tool call by data trust level at access time.
 * Emits: PRIVILEGED_DATA_ACCESSED signal.
 *
 * Depends on: src/types/signals.ts
 */

export {};
