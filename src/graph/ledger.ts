/**
 * Provenance Ledger — SQLite-backed provenance store.
 *
 * Tags every memory write with source, trust level, timestamp,
 * session ID, and content hash.
 *
 * Depends on: better-sqlite3, src/types/signals.ts
 */

export {};
