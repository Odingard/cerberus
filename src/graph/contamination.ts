/**
 * Contamination Graph — Directed taint propagation graph.
 *
 * Tracks how contaminated data flows through memory writes
 * and reads across sessions.
 *
 * Depends on: src/types/signals.ts
 */

export {};
