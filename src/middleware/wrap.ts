/**
 * Developer-facing API — cerberus.guard() entry point.
 *
 * This is the one-line integration surface for Cerberus.
 * Wraps an existing agent with all 4 detection layers
 * and the correlation engine.
 *
 * Depends on: src/layers/*, src/engine/*, src/types/*
 */

export {};
