/**
 * Attack Harness — 3-tool agent with OpenAI function calling.
 *
 * This is the research instrument. It runs a function-calling loop
 * with three tools (readPrivateData, fetchExternalContent, sendOutboundReport)
 * and logs every execution turn as a structured trace.
 *
 * Depends on: openai, harness/tools.ts
 */

export {};
