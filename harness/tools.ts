/**
 * Attack Harness — Tool A, B, C definitions.
 *
 * Tool A — readPrivateData(): reads sensitive records from local JSON.
 * Tool B — fetchExternalContent(url): HTTP GET to a controlled URL.
 * Tool C — sendOutboundReport(data): POSTs to a webhook endpoint.
 *
 * Each tool includes metadata emission hooks for signal capture.
 */

export {};
