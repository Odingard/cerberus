/**
 * Automated Attack Executor.
 *
 * Runs the agent against each payload variant, captures structured
 * JSON traces, and labels each run with success/failure classification.
 *
 * Output: harness/traces/*.json
 *
 * Depends on: harness/agent.ts, harness/payloads.ts
 */

export {};
